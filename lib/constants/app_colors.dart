import 'package:flutter/material.dart';

// All app colors live here. To change the entire app's color scheme,
// you only need to edit this one file.
class AppColors {
  static const Color primary = Color(0xFFD32F2F);       // deep red
  static const Color secondary = Color(0xFFEF5350);     // lighter red
  static const Color background = Color(0xFFFDF5F5);    // very light pink-white
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;
  static const Color black = Colors.black87;
  static const Color success = Color(0xFF2E7D32);       // green for "available" badge
  static const Color warning = Color(0xFFF57F17);       // orange for "urgent"
  static const Color cardBackground = Color(0xFFFFFFFF);
}
